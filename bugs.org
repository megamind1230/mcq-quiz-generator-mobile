#+TITLE: bugs.org
#+DESCRIPTION: Known bugs and fix strategies for MCQ Quiz (Mobile PWA)

* Current Bugs
  - [O] No native "Open with..." support.  Students must open the in-app
        file picker manually.  Native intent handling requires Capacitor/APK
        build (deferred -- see plan-todos.org Phase 8).
  - [O] No settings UI screen.  Settings (shuffle, instantFeedback) retain
        defaults; wired in SettingsContext but not exposed in the UI yet.

* Fixed Bugs
  - [X] TS2322 in crypto.ts decryptMcqPayload (v0.1.0) -- params typed
        =Uint8Array= (ArrayBufferLike) failed BufferSource check.  Fixed by
        typing params as =Uint8Array<ArrayBuffer>=.
