#+DESCRIPTION: MCQ Quiz (Mobile) - Offline student quiz taker PWA

* MCQ Quiz (Mobile)

A ~100% offline progressive web app (PWA) for *students only*.  It imports
=.mcq= and =.emcq= quiz files (created by the desktop app
=mcq-quiz-generator-desktop=), lets students take the quiz, shows a graded
results page, logs results locally on the device, and can export a PDF.

This app deliberately does *not* generate, encrypt, or author quizzes -- that
is left to the desktop app.  It only parses and grades.  This keeps it small,
offline-only, and safe to hand out to students: they never need an account,
an API key, or network access after first load.

* Live Demo
  https://megamind1230.github.io/mcq-quiz-generator-mobile/

* Screenshots
  (none yet)

* Features

** Import
  - "Open Exam" button opens the device file picker
  - Accepts =.mcq=, =.emcq=, =.txt=, =.md=, =.org= files
  - =.emcq= files are decrypted in the browser with WebCrypto
    (AES-256-GCM), using the same key as the desktop app -- no plaintext
    temp files are ever written
  - Rejects invalid or empty files; caps at 50 questions
  - Remembers and displays the last opened file name on the home screen

** Quiz
  - One question at a time with clickable option cards
    - *Single answer:* radio markers (=●=/=○=), "Single answer" tag
    - *Multi answer:* checkbox markers (=☑=/=☐=), "Select all that apply"
      tag and a hint; graded by exact match (all-or-nothing)
  - *Mode picker:* after selecting a file, choose from three modes:
    - *Loop Mode:* instant feedback, retakes wrong answers in batches of 15
      until all mastered, no results page
    - *Instant Mode:* instant feedback on every answer, no results page
    - *Classic Mode:* no instant feedback (deferred to results), full results
      summary at the end with per-question review
  - Count-up timer
  - Answer locking: in Loop and Instant modes, once an answer is submitted it
    cannot be changed (prevents cheating)
  - Optional options shuffle and question-order shuffle (settings)
  - Explanation display after each answer
  - Next / Previous navigation
  - =← Leave= button (red) with confirm dialog
  - Can't finish until every question is answered
  - Keyboard shortcuts: =a= =b= =c= =d= (select), =n= =p= (next/prev),
    =j= =k= (scroll), =Esc= / =q= (leave)

** Results
  - Percentage score with color coding (green/orange/red)
  - Per-question review: *Your Answer(s) ==>* / *Correct Answer(s) ==>* with
    ✅/❌ (green/red) and full Explanation
  - Only Classic mode saves results to history and shows the results page
  - Loop and Instant modes return to the home screen when finished
  - History list on the Home screen with title, score, time, and date

** Export
  - =Export PDF= button -- opens a self-contained HTML result page and calls
    the browser's native "Save as PDF" print dialog (works offline)
  - KaTeX math and highlighted code render in the exported HTML

** Rich Content
  - LaTeX math (KaTeX): inline =\$...\$= and display =\$\$...\$\$=
  - Code syntax highlighting (highlight.js): fenced blocks + inline
  - Click-to-copy on code/math elements

** Example

  #+BEGIN_SRC markdown
  ---
  title: Physics Quiz
  source: physics.md
  generated: 2026-08-01 10:00
  ---

  ## Question 1
  What is the integral of $f(x) = 3x^2$ from 0 to 1?

  A. 0
  B. 1
  C. 2
  D. 3

  **[Answer: B]**
  **Explanation:** $\int_0^1 3x^2\,dx = \left[x^3\right]_0^1 = 1$.

  ---

  ## Question 2
  What does this Python function return?

  ```python
  def fib(n):
      if n <= 1:
          return n
      return fib(n - 1) + fib(n - 2)
  ```

  A. The sum of 1 to n
  B. The nth Fibonacci number
  C. The factorial of n
  D. The nth prime number

  **[Answer: B]**
  **Explanation:** =fib(n)= recursively computes Fibonacci numbers: =fib(0)=0=, =fib(1)=1=, =fib(n)=fib(n-1)+fib(n-2)=.

  ---

  ## Question 3
  Which of the following are prime numbers?

  A. 7
  B. 9
  C. 13
  D. 15

  **[Answer:A,C]**
  **Explanation:** 7 and 13 are prime. 9 = 3x3 and 15 = 3x5 are composite.
  #+END_SRC

** Settings
  Persisted in =localStorage=, accessible via the gear button in the topbar:
  | Setting               | Default | Purpose                             |
  |-----------------------+---------+-------------------------------------|
  | Theme                 | dark    | Dark / light mode                   |
  | Randomize Options     | on      | Shuffle option order each quiz      |
  | Randomize Q Order     | on      | Shuffle question order each quiz    |

** PWA / Offline
  - Installable: add to Home Screen (standalone display)
  - Service worker precaches all app assets (KaTeX fonts included) for full
    offline use
  - Auto-updates on next load after a deploy

* Prerequisites
  - Node.js 18+ and npm (only to build/host)
  - No account, no API key, no backend

* Quick Start

  #+BEGIN_SRC bash
  cd mcq-quiz-generator-mobile
  npm install
  npm run dev            # local dev server
  npm run build          # production build in dist/
  #+END_SRC

  To distribute:
  1. =npm run build=
  2. Host the =dist/= folder on any static host (GitHub Pages, Netlify, Vercel)
  3. Send students the URL; they open it once and "Add to Home Screen"
  4. After first load it works fully offline
  5. Students download the =.mcq=/=.emcq= file (Telegram, WhatsApp, email, USB)
     and open it with the in-app *Open Exam* button

* Available Scripts

  | Command             | Description                       |
  |---------------------+-----------------------------------|
  | =npm run dev=       | Start development server           |
  | =npm run build=     | Build production PWA into =dist/=  |
  | =npm run preview=   | Preview the production build       |
  | =npm run typecheck= | Type-check app + vite config       |
  | =npm test=          | Run unit tests (vitest)            |

* Logs

  - Quiz results: =localStorage= (key =quiz-results.json=), per device
  - Settings: =localStorage= (key =mcq-settings.json=), per device
  - No server-side logs (fully client-side)

* Tech Stack

  | Layer          | Tech                  | Version |
  |----------------+-----------------------+---------|
  | UI framework   | React + TypeScript    | ^19     |
  | Build tooling  | Vite (react plugin)   | ^5      |
  | PWA            | vite-plugin-pwa       | ^0.21   |
  | Math rendering | KaTeX                 | ^0.18   |
  | Code highlight | highlight.js          | ^11     |
  | Crypto         | WebCrypto (AES-GCM)   | native  |
  | Unit tests     | vitest                | ^4      |

* How It Works

  1. *Open:* user taps *Open Exam* and picks a file (or drags onto the picker)
  2. *Decrypt:* if the file is =.emcq= (or starts with ~encrypted-mcq-v1~),
     it is decrypted in-browser via WebCrypto AES-GCM with the shared key
  3. *Parse:* =mcqParser= turns the text into an =McqDocument=
     (questions + options + =correctIndices=)
  4. *Shuffle (optional):* options and/or question order are randomized
  5. *Quiz:* =QuizView= renders one question at a time, records =selectedIndices=
  6. *Grade:* exact-match set equality per question -> score
  7. *Log + export:* result saved to =localStorage=; PDF via =buildHtml+
     =exportAssets= then =window.print()=

* Project Structure

  #+BEGIN_SRC
  mcq-quiz-generator-mobile/
  ├── public/
  │   └── icons/            # PWA icons (32/180/192/512 PNG)
  ├── src/
   │   ├── App.tsx                # Root component, topbar + theme toggle + gear
   │   ├── SettingsContext.tsx    # localStorage settings context
   │   ├── main.tsx               # Entry: KaTeX/hljs CSS + SettingsProvider
   │   ├── types.ts               # Shared types (McqDocument, McqQuestion, ...)
   │   ├── styles.css             # Quiz theme / layout CSS
   │   ├── components/
   │   │   ├── QuizView.tsx       # Home, OpenExam, active quiz, results, PDF
   │   │   └── SettingsPanel.tsx  # Settings modal (gear button overlay)
   │   ├── lib/
   │   │   └── crypto.ts          # WebCrypto AES-GCM .emcq decrypt
   │   ├── utils/
   │   │   ├── cssAssets.ts       # Inlined KaTeX/hljs CSS for exports
   │   │   ├── format.ts          # Shared: formatTime, escapeHtml, LABELS
   │   │   ├── mcqParser.ts       # .mcq text -> McqDocument
   │   │   ├── quizModes.ts      # Loop mode logic + grading + QuizMode type
   │   │   ├── quizExporter.ts    # results -> HTML/TXT/MD/Org
   │   │   ├── quizLogger.ts      # localStorage result history
   │   │   ├── renderRichText.ts  # KaTeX + highlight.js rendering
   │   │   └── shuffle.ts         # Fisher-Yates shuffle
  │   └── __tests__/              # Unit tests + fixture .mcq/.emcq
  ├── vite.config.ts        # Vite + PWA manifest config
  ├── tsconfig.json / tsconfig.node.json
  ├── plans-and-todos.org    # Implementation plan
  ├── bugs.org              # Known bugs and fixes
  ├── changelog.org          # Version history
  └── README.org
  #+END_SRC

* Companion Desktop App

  The =mcq-quiz-generator-desktop= sibling project generates, encrypts, and
  authors =.mcq=/=.emcq= quizzes (with Gemini AI generation).  Share the
  resulting files with students; this mobile app is how they take them.
